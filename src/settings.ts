

/**
 * Base HH service URL
 */
export const HH_API_BASE_URL = 'https://api.hh.ru';

/**
 * Default timeout for quering HH service
 */
export const DEFAULT_REQUEST_TIMEOUT = 8000; // ms

/**
 * Max page size for quering HH service
 */
export const PAGE_SIZE = 100; // ms

/**
 * Max amount of vacancies that could be retrieved from HH API
 */
export const MAX_VACANCIES_AMOUNT = 2000;

export const DEFAULT_USER_AGENT = 'hh-stats npm package'